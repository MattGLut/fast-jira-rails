<div class="mx-auto max-w-md rounded-2xl border border-gray-700 bg-gray-800/90 p-6 shadow-2xl shadow-black/40">
  <h2 class="mb-1 text-2xl font-semibold text-gray-100" style="font-family: 'Playfair Display', serif;">Change your password</h2>
  <p class="mb-6 text-sm text-gray-300">Set a new secure password for your account.</p>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put, class: "space-y-4" }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>
    <%= f.hidden_field :reset_password_token %>

    <div>
      <div class="mb-1 flex items-center justify-between text-sm">
        <%= f.label :password, "New password", class: "font-medium text-gray-200" %>
        <% if @minimum_password_length %>
          <span class="text-xs text-gray-400"><%= @minimum_password_length %> chars min</span>
        <% end %>
      </div>
      <%= f.password_field :password, autofocus: true, autocomplete: "new-password", class: "w-full rounded-lg border border-gray-600 bg-gray-700 px-3 py-2 text-white focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-500/40" %>
    </div>

    <div>
      <%= f.label :password_confirmation, "Confirm new password", class: "mb-1 block text-sm font-medium text-gray-200" %>
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "w-full rounded-lg border border-gray-600 bg-gray-700 px-3 py-2 text-white focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-500/40" %>
    </div>

    <%= f.submit "Change my password", class: "w-full rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white hover:bg-indigo-700" %>
  <% end %>

  <div class="mt-5 space-y-1 text-sm text-gray-300">
    <%= render "devise/shared/links" %>
  </div>
</div>
